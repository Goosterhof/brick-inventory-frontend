# Inspector's Casebook

_The Inspector's private notebook. Suspicions, patterns, and unresolved threads that carry across inspections. The Pulse tracks what the CFO acknowledges. The Casebook tracks what the Inspector notices._

_Read this before every inspection. Write to it after every inspection. Suspicions that prove unfounded get crossed out with a reason — they don't get deleted. The Inspector's memory includes what it got wrong._

---

## Standing Suspicions

_Areas the Inspector is watching. Each entry includes when the suspicion started, what triggered it, and what to look for next time._

| Area                                     | Suspicion                                                                                                                                                                                                              | First Noticed | Evidence So Far                                                                                                                                                                                                                                                                                                     | Next Inspection: Look For                                                                                                                         |
| ---------------------------------------- | ---------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- | ------------- | ------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- | ------------------------------------------------------------------------------------------------------------------------------------------------- |
| ~~Brick Catalog~~                        | ~~Domain map references `brick-catalog.md` but file was not located in `.claude/docs/`. Either the file is missing entirely or moved.~~                                                                                | 2026-03-24    | **Closed 2026-03-27.** CEO deleted brick-catalog.md (superseded by ADR-009 component health registry). All references cleaned in PR #133: domain-map link removed, `lint:catalog` script removed, `validate-brick-catalog.mjs` deleted. Permit 2026-03-26-brick-catalog-blueprint voided.                           | N/A — resolved.                                                                                                                                   |
| ComponentHealth registry freshness       | `component-registry.json` fails format:check — content appears correct but formatting does not match oxfmt output.                                                                                                     | 2026-03-24    | Fails on 2026-03-24, 2026-03-25, 2026-03-26, and 2026-03-29 (four consecutive inspections). Content accurate (32 components). Format only. Escalated to Recurring Patterns at 3 occurrences per casebook rule.                                                                                                      | Continue monitoring. If generation pipeline is not fixed, this will never resolve.                                                                |
| SetsOverviewPage slow test               | `SetsOverviewPage.spec.ts` exceeded 1000ms hard threshold, blocking the pre-push gauntlet                                                                                                                              | 2026-03-24    | Resolved: Theme Atlas split SetsOverviewPage tests into two files. `SetsOverviewTheme.spec.ts` now at 967-998ms (warning zone, not failure). `SetsOverviewPage.spec.ts` appears stable. `ComponentGallery.spec.ts` remains the active 1000ms violator.                                                              | Monitor `SetsOverviewTheme.spec.ts` — it could cross the 1000ms threshold. `ComponentGallery.spec.ts` still unresolved.                           |
| Coverage exclusion scope                 | `src/apps/showcase/**` is excluded from vitest coverage thresholds with no comment or ADR. Silent carve-outs like this tend to expand — check if other app paths have been quietly added to the exclude list over time | 2026-03-24    | Showcase tests now written — coverage exclusion may be less relevant. Vitest config exclude list appears unchanged (no new app paths added).                                                                                                                                                                        | Verify on next inspection if showcase coverage is now included.                                                                                   |
| ~~`lint:catalog` broken script~~         | ~~`npm run lint:catalog` crashes with ENOENT because `brick-catalog.md` doesn't exist. This is a documented npm script that is silently broken.~~                                                                      | 2026-03-25    | **Closed 2026-03-27.** Same root cause as Brick Catalog suspicion above. Script and file both removed in PR #133. No longer in package.json.                                                                                                                                                                        | N/A — resolved.                                                                                                                                   |
| `ComponentGallery.spec.ts` collect guard | ComponentGallery.spec.ts now exceeds both the collect duration threshold (1275ms delta) and the 1000ms execution test guard (1091ms→761ms). This blocks the pre-push gauntlet.                                         | 2026-03-25    | ADR-010 violation. Still failing on 2026-03-29 (1298ms collect delta). The execution guard appears to have dropped below 1000ms. Root cause unresolved.                                                                                                                                                             | Check whether collect guard and execution guard failures persist; root cause may be `mount` (not `shallowMount`) importing all shared components. |
| Pulse quality metrics staleness          | Pulse Quality Metrics table shows test file count and test count that do not match current gauntlet output.                                                                                                            | 2026-03-25    | Still severely stale on 2026-03-29: Pulse shows 1081 tests (actual: 1342), 90 test files (actual: 97 unit), 12 ADRs (actual: 13), 12 showcase components (actual: 15), 7 domains (actual: 8). Filed as Finding 6 (medium) in 2026-03-29 report. Four journals proposed updates; none landed. This is a process gap. | Verify whether Pulse metrics are updated after this inspection. If not, escalate to high severity — 4 consecutive misses.                         |
| ADR-004 case conversion drift            | `useValidationErrors.ts` imports `deepCamelKeys` directly from `string-ts` instead of the documented `@shared/helpers/string` wrapper.                                                                                 | 2026-03-29    | Confirmed: line 5 of `src/shared/composables/useValidationErrors.ts`. Filed as Finding 2 (medium) in 2026-03-29 report. The rebuttal-fixes delivery explicitly scoped out shared service files. This is the remaining gap.                                                                                          | Verify whether this file is corrected.                                                                                                            |
| SettingsPage.spec.ts collect guard       | SettingsPage.spec.ts is breaching the 1000ms collect guard (1060ms delta). New violation created by member removal wrench delivery.                                                                                    | 2026-03-29    | Confirmed: file grew to 960 lines / 48 tests. Filed as Finding 3 (medium) in 2026-03-29 report. Root cause: 10 new tests + complex mock setup from member removal feature.                                                                                                                                          | Verify whether file is split. If another delivery adds more tests before the split, it will breach further.                                       |
| Vue Router next() deprecation            | Router service uses deprecated three-argument `(to, from, next)` guard pattern instead of return-value pattern, emitting 12+ deprecation warnings per test run.                                                        | 2026-03-29    | Confirmed: `src/shared/services/router/index.ts` lines 31–38. Vue Router 5.0.3. Filed as Finding 1 (medium). Pre-existing but now generating visible warning noise.                                                                                                                                                 | Verify whether the guard is updated to use return-value pattern.                                                                                  |

## Recurring Patterns

_Findings that keep resurfacing in the same area. Two occurrences is a coincidence. Three is a structural problem._

| Area                           | Pattern                                                                                                                                                                                                                                              | Occurrences | Last Seen  | Escalated to Pulse?                                                                                                                                                                   |
| ------------------------------ | ---------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- | ----------- | ---------- | ------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- |
| Documentation counts           | Component counts and test counts in docs (domain-map, pulse) consistently lag behind actual codebase counts. Happened on every inspection.                                                                                                           | 4           | 2026-03-29 | Partially — Pulse metrics flagged as stale in proposed updates each inspection, but not updated between inspections. Structural gap: no automated sync from gauntlet output to Pulse. |
| component-registry.json format | JSON file fails format:check on every inspection run. Pre-push gauntlet blocked by this for 4 consecutive inspections.                                                                                                                               | 4           | 2026-03-29 | Yes (Pulse: Active Concern) — recommend escalating to Medium severity per 3-occurrence rule.                                                                                          |
| Pre-push gauntlet blocked      | Gauntlet fails on every inspection — SetsOverviewPage (2026-03-24), ComponentGallery (2026-03-25 and 2026-03-26 and 2026-03-29), SettingsPage.spec.ts (2026-03-29 new). Different or recurring cause each time; structural issue with collect guard. | 4           | 2026-03-29 | Yes (Pulse: Active Concern)                                                                                                                                                           |
| Domain map underdocumented     | Domains exist in production code that are not in the domain map. `brick-dna` found this inspection (fully active domain, zero documentation). Pattern: domain map treated as optional, not required, by architects during domain creation.           | 1           | 2026-03-29 | No — first occurrence. Needs one more before escalation.                                                                                                                              |

## Rebuttal Lessons

_Successful rebuttals from the Architect that revealed gaps in the Inspector's methodology. Each entry is a calibration — the Inspector approached something wrong and the Architect proved it._

| Date         | Finding Rebutted | Architect's Evidence | What I Missed | Adjusted Approach |
| ------------ | ---------------- | -------------------- | ------------- | ----------------- |
| _(none yet)_ |                  |                      |               |                   |

## Crossed-Out Suspicions

_Suspicions that were investigated and found to be unfounded. Kept for institutional memory — the Inspector doesn't forget what it got wrong._

| Area                                            | Original Suspicion                                                               | Investigated | Conclusion                                                                                                                                                                                                                |
| ----------------------------------------------- | -------------------------------------------------------------------------------- | ------------ | ------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- |
| `defineComponent` in DialogServiceDemo          | Suspected Options API violation (not using `<script setup>`)                     | 2026-03-24   | Not a violation — `DemoDialogContent` is rendered programmatically via `h()` function. `<script setup>` is not applicable for render-function components. Intentional pattern.                                            |
| `style="..."` inline in AntiPatterns            | Suspected ADR-003 violation (inline style, not UnoCSS)                           | 2026-03-24   | Not a violation — intentional anti-pattern demonstration. The entire point of the "Wrong" panels is to show what NOT to do.                                                                                               |
| `app.provide("weight"/"size"/"color")` orphaned | Suspected dead code in showcase main.ts (provide with no inject consumers found) | 2026-03-24   | Not dead code — standard Phosphor Icons configuration pattern. Same three lines appear in families/main.ts. Library uses these injected values internally.                                                                |
| `console.error` in storage.ts                   | Suspected no-console violation per CLAUDE.md                                     | 2026-03-25   | Not a violation — `.oxlintrc.json` has an explicit override disabling `no-console` and `no-restricted-globals` for `src/shared/services/storage.ts`. Intentional exception for a service that IS the storage abstraction. |
| `@/apps/` import paths in showcase tests        | Suspected violation of CLAUDE.md import path rules                               | 2026-03-25   | Not a violation — `.oxlintrc.json` overrides `no-restricted-imports: "off"` for all `src/tests/**` files. Test files have different import rules than production code.                                                    |
